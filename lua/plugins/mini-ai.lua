-- Text objects extra: argumento (a), llamada a función (f), tag (t), etc.
-- Uso: dia / cia (argumento), daf / cif (llamada a función), vit (dentro de tag).
-- n_lines limita cuántas líneas mira hacia los lados al buscar el objeto.
require("mini.ai").setup({ n_lines = 500 })
